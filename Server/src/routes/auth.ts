// src/routes/auth.ts
import { Router } from "express";

const router = Router();

//  Nota:
// Las rutas de registro y login ya están implementadas en index.ts.
// Este archivo se mantiene vacío para futuras expansiones o middlewares.

export default router;
